//JS Program to check if a number is multiple of 3 or not using a ternary operator?

var a=parseInt(prompt("Enter a number"));

var result=(a%3==0)?"The number is multiple of 3" : "The number is not multiple of 3";

console.log(result);